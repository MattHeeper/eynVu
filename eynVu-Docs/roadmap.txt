┌─────────────────────────────────────────────────┐
│  فاز 1: پایه‌گذاری (هفته 1-2)                   │
│  فاز 2: پیام ناشناس (هفته 3-4)                 │
│  فاز 3: سیستم کاربری (هفته 5-6)                │
│  فاز 4: کافه و محتوا (هفته 7-8)                │
│  فاز 5: پنل ادمین (هفته 9-10)                  │
│  فاز 6: مدیریت گروه (هفته 11-14)               │
└─────────────────────────────────────────────────┘

eynVu/
├── main.py              # نقطه شروع
├── config.py            # تنظیمات
├── database.py          # کانکشن دیتابیس
├── .env                 # متغیرهای محیطی
└── requirements.txt     # وابستگی‌ها ✅

models/
├── __init__.py
├── user.py              # مدل کاربر
├── identifier.py        # شناسه Ua...
└── log.py               # ثبت وقایع
```

#### **1.4 - سیستم شناسه اختصاصی**
- ساخت تابع تولید `Ua1@gb2h`
- ذخیره در دیتابیس
- بررسی یکتا بودن

#### **1.5 - منوی اصلی**
```
🏠 eynVu Bot
├── 📨 ارسال نامه
├── ☕ کافه eynVu
├── 🏆 لیدربورد
├── 📋 لیست‌ها
├── 🔗 سوشال مدیا
└── 👤 پروفایل من
```

#### **1.6 - اولین قابلیت: ارسال ناشناس ساده**
- کاربر پیام می‌فرسته
- تایید (آره/نه)
- ارسال به شما (ادمین)

---

[x] 1. نصب PostgreSQL
[x] 2. راه‌اندازی Python environment
[x] 3. تست اتصال دیتابیس
[x] 4. ساخت config.py
[x] 5. ساخت database.py
[x] 6. ساخت models/user.py ✅
[x] 7. ساخت models/identifier.py ✅
[x] 8. ساخت models/log.py ✅
[x] 9. تست مدل‌ها
[ ] 10. ساخت handlers/start.py  ← بعدی

---

## 🔥 **فاز 2: سیستم پیام ناشناس** (بعدی)

### **قابلیت‌ها:**
1. ✅ ارسال ناشناس به شما
2. ارسال ناشناس به ادمین
3. ارسال ناشناس به کاربر (با شناسه)
4. پاسخ به پیام
5. بلاک کاربر
6. کیک کاربر
7. مسدود تایمی
8. حذف پیام (از چت / از دیتا)
9. نام مستعار
10. ارسال زمان‌بندی شده

---

## 🎨 **فاز 3: سیستم کاربری**

### **قابلیت‌ها:**
1. پروفایل کاربر
2. لیدربورد
3. سیستم تایتل (رول)
4. کاربران ویژه (VIP)
5. نمایش آمار
6. ثبت وقایع کامل

---

## ☕ **فاز 4: کافه و محتوا**

### **قابلیت‌ها:**
1. **میز رادیو:**
   - ساخت ایستگاه
   - پخش محتوا
   - لیدربورد ایستگاه‌ها

2. **میز کتابخانه:**
   - اضافه کردن کتاب
   - نمایش کتاب با قالب خاص
   - دسته‌بندی

3. **میز پلی‌لیست:**
   - ساخت پلی‌لیست (تا 9 آهنگ)
   - مدیریت آهنگ‌ها

4. **گوشه گالری:**
   - اسلایدشو خودکار (هر 15 دقیقه)

---

## 👑 **فاز 5: پنل ادمین**

### **قابلیت‌ها:**
1. داشبورد اصلی
2. مدیریت کاربران
3. مدیریت محتوا (کتاب، گالری، بیلبورد)
4. پست چنل
5. تنظیمات ربات

---

## 🛡️ **فاز 6: مدیریت گروه**

### **قابلیت‌ها:**
1. خوش‌آمدگویی
2. 28 نوع قفل
3. سیستم اخطار/بن/سکوت
4. کپچا (تایید هویت)
5. ضد اسپم
6. قفل خودکار
7. مدیریت مدیران

---

## 🚀 **الان از کجا شروع کنیم؟**

### **پیشنهاد من: بریم سراغ تسک 4 از چک‌لیست فاز 1**
```
بعدی: ساخت config.py
